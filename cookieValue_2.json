{
    "bambda":{
        "http_history_display_filter":{
            "bambda":"var request = requestResponse.request();\r\nvar cookie = request.parameter(\"pasteurparamName\", HttpParameterType.COOKIE);\r\n\r\nreturn cookie != null && cookie.value().contains(\"pasteUrcookieValue\");"
        }
    }
}