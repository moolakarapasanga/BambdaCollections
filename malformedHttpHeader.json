{
    "bambda":{
        "http_history_display_filter":{
            "bambda":"return requestResponse.response().headers().stream().anyMatch(\r\n     e -> e.name().contains(\" \"));"
        }
    }
}